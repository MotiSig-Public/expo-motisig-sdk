# Expo / Metro resolve packages from a hoisted node_modules layout (recommended for pnpm + RN).
node-linker=hoisted

# pnpm 10+ may prompt before purging node_modules on version bumps; non-TTY/CI needs this off.
confirm-modules-purge=false
